version: '3'
services:
  tomcat_asr_agent:
    build:
      context: .
      dockerfile: Dockerfile.asist
    ports:
      - 8888:8888
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/google_application_credentials.json
    volumes:
      - $GOOGLE_APPLICATION_CREDENTIALS:/google_application_credentials.json
      - .:/asr
